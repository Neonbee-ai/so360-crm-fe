export const useFormatters = () => ({
  formatCurrency: (v) => String(v),
  formatDate: (v) => String(v),
  formatNumber: (v) => String(v),
});
export const formatCurrency = (v) => String(v);
export const formatDate = (v) => String(v);
export default {};
